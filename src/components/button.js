import styled from 'styled-components'import {  position,  compose,  color,  size,  flexbox,  space,  layout,  borderRadius} from 'styled-system'import { TouchableOpacity } from 'react-native'const Button = styled(TouchableOpacity)(  compose(layout, color, size, space, flexbox, borderRadius, position))Button.defaultProps = {  flexDirection: 'row',  alignItems: 'center',  justifyContent: 'center'}export default Button